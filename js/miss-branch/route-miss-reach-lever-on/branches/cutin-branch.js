import { routeMissReachLeverOnCutInBranch } from '../cutin/index.js';

// 外れ時リーチ分岐向けカットイン演出の分岐先処理です。
export const runMissReachLeverOnCutInBranch = (detail) => {
  routeMissReachLeverOnCutInBranch(detail);
};
